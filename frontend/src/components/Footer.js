export default function Footer() {
  return (
    <footer className="px-8 py-6 bg-gray-950 border-t text-sm text-gray-400 text-center">
      [Built by the IT intern team — TaskFlow ({new Date().getFullYear()})]
    </footer>
  );
}